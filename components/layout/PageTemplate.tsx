import MainLayout from './MainLayout'

/**
 * Shared page template component for all dashboard pages.
 * Provides consistent layout structure across all pages.
 */
export default function PageTemplate() {
  return <MainLayout />
}

